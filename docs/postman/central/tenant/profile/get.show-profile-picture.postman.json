{
  "item": {
    "name": "Show Profile Picture",
    "request": {
      "method": "GET",
      "header": [
        {
          "key": "Accept",
          "value": "application/json"
        }
      ],
      "url": {
        "raw": "{{base_url}}/api/profile-pictures/{{profile_picture_id}}",
        "host": [
          "{{base_url}}"
        ],
        "path": [
          "api",
          "profile-pictures",
          "{{profile_picture_id}}"
        ]
      }
    }
  },
  "order": 30
}