{
  "item": {
    "name": "Deactivate User",
    "request": {
      "method": "DELETE",
      "header": [
        {
          "key": "Accept",
          "value": "application/json"
        }
      ],
      "url": {
        "raw": "{{base_url}}/{{tenant_slug}}/api/users/{{tenant_user_id}}",
        "host": [
          "{{base_url}}"
        ],
        "path": [
          "{{tenant_slug}}",
          "api",
          "users",
          "{{tenant_user_id}}"
        ]
      }
    }
  },
  "order": 10
}