; ============================================================================
; Asterisk HTTP Server Configuration
; ============================================================================
; Required for ARI REST API and WebSocket events.

[general]
enabled = yes
bindaddr = 0.0.0.0
bindport = 8088
